# Time complexity of accessing an element of a list is O(1)
# TC of inserting or deleting at head is O(n)
# TC of inseting or deleting at tail is O(1) if using .append(i) and .pop(i) methods. O(n) if done as a data structure array
